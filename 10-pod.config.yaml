apiVersion: v1
kind: Pod-config

metadata: 
 name: config-map
 namespace: roboshop

spec:
 containers:
   - name: nginx
     image: nginx
     envFrom:
     - configMapRef:
       name: config-demo
       